# --- CONECTARE BAZ? DE DATE ---
spring.datasource.url=jdbc:postgresql://localhost:5432/notes_app
spring.datasource.username=admin
spring.datasource.password=password_securizata

# --- SET?RI JPA / HIBERNATE ---
# IMPORTANT: Set to 'create' to fix the UUID column type issue
# After first successful run, change back to 'update'
spring.jpa.hibernate.ddl-auto=update

# SQL logging for debug
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- SET?RI THYMELEAF (FRONTEND) ---
spring.thymeleaf.cache=false